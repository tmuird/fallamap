# PLAN.md — fallamap overnight refinement

**STATUS: IN PROGRESS** (target: every acceptance criterion below VERIFIED)

Mission: make fallamap work flawlessly — every flow functional, zero console errors,
clean build + lint, graceful degradation, and deployment streamlined so a fresh clone
deploys with documented env vars. Product direction: white-label event social network
(event data config-driven, deployable per event/itinerary).

## Acceptance criteria (definition of "flawless")

- [x] `npm run build` passes (tsc clean, no vite errors)
- [x] `npm run lint` passes with zero warnings (verified 04:58 via T1.3 — `eslint src --report-unused-disable-directives --max-warnings 0`, 0 problems)
- [ ] Every route (/, /map, /schedule, /archive, /contact, /profile, /sign-in, /sign-up, /dashboard) renders with zero console errors
- [ ] Map: renders tiles, shows monument markers from fallas.json, marker → drawer open/close works repeatedly without pointer-event or stacking interference, no blank canvas on bad/missing token (graceful fallback)
- [ ] Auth: sign-up / sign-in / sign-out flows work end to end (Clerk)
- [ ] Photo upload flow works against Supabase (or degrades with a clear user-facing message)
- [ ] Schedule page renders official events correctly from data
- [ ] Profile: activity, collection, passport views render and load user data safely (no crash when logged out / no data)
- [ ] Admin moderation dashboard renders and its review actions behave
- [ ] Responsive layout: no horizontal scroll / broken overlays on laptop + mobile widths
- [ ] Deployment: README rewritten (real project README, setup, env vars, deploy steps), `.env.example` accurate, fresh-clone → build → deploy path documented and verified
- [ ] White-label readiness: event-specific strings/data all sourced from data/config files (no new hardcoded Valencia/Fallas strings in components)
- [ ] All work committed on `overnight-polish`, pushed to origin, merge-ready (no conflicts with main)

> Backend-dependent criteria (photo upload, moderation, live user data, end-to-end auth sync) can
> only be fully verified against a live Supabase project — currently unreachable (see Blockers).
> Until then: code must be ready, tested against the schema in `scripts/schema.sql`, and degrade
> with a clear user-facing state.

## Task list

### P0 — broken / blocking
- [x] T0.1 App resilience: error boundary around routes; wrap `mapboxgl.Map` init in try/catch + `mapboxgl.supported()` gate with static "map needs WebGL" fallback (AUDIT §1 — whole app white-screens on map init failure)
- [x] T0.2 `scripts/schema.sql` + seed migration: comments/images/image_likes/user_interactions tables, community-content storage bucket + policies, event_id/hub_id columns, FKs for embedded selects; seed script runs schema first (AUDIT §4) — verified locally via `npm run verify:schema`
- [x] T0.3 Clerk↔Supabase auth wiring: supabase-js `accessToken` callback + SupabaseAuthBridge (Clerk `session.getToken()`; the plan's `template: 'supabase'` path is deprecated since Apr 2025 — see docs/supabase-clerk-auth.md for the third-party-auth setup), RLS keyed on Clerk `sub` in scripts/schema.sql | verified via `npm run verify:schema` + browser runtime probe; live signed-in E2E needs a live Supabase project (Blockers); moderation admin path deferred to T1.2

### P1 — major
- [x] T1.1 Graceful degradation: community-offline state when Supabase is unreachable — reachability probe (src/lib/backendStatus.ts, HEAD probe + 45s retry + browser-online event), global CommunityOfflineBanner, offline-aware empty states in FallaDetails | browser-verified both offline→online→offline transitions on dev server
- [x] T1.2 Real moderation pipeline: hooks insert `status:'pending'` (useFallaDetails/useEventDetails); admin fetches gated behind `isLoaded && isAdmin`; server-side admin enforcement in RLS (`public.is_admin()` on the Clerk claim `public_metadata.role`, status-only column-level UPDATE grants, non-admin inserts forced to 'pending') | verified via `npm run verify:schema` (new admin/queue/self-approve/forged-claim cases all pass) + browser: /dashboard signed-out fires zero moderation queries and zero console errors | live signed-in admin E2E unverified (needs live Supabase + the Clerk session-token claim from docs/supabase-clerk-auth.md)
- [x] T1.3 Make `npm run lint` work: ESLint 9 flat config (`eslint.config.js`) replaces `.eslintrc.cjs`, missing lint devDeps installed (typescript-eslint, eslint-plugin-react-hooks/-refresh, globals), script drops `--ext` | `npm run lint` passes CLEAN (0 problems, `--max-warnings 0`, exit 0); `npm run build` green; browser smoke /map + /schedule | deviations in eslint.config.js comments: storybook preset dropped (no stories), `no-explicit-any` off until T3.5, react-compiler-era react-hooks rules not enabled (11 pre-existing hits = behavioural refactor, see worklog)
- [x] T1.4 fallas.json data model + number-keyed sync: every entry gains `id` (`falla-<number>`)/`description`/`is_special`/`is_burnt` (honest defaults, no fabricated data — fields now mirror the DB columns); seed upserts the new columns; MapComponent merges DB rows over local per `number` (missing fields fall back, AUDIT §3 note); "Special" filter is data-driven — hidden when no monument is flagged, no more "0 of 85"; FallaDetails user_interactions reads/writes use the `number`→DB-id resolution (useFallaDetails exposes `dbId`, reset per entity) and localStorage stays keyed on `number` (AUDIT §2) | verified via `npm run verify:schema` (ALL SCHEMA CHECKS PASSED: enriched-columns + number-resolved interaction cases, seed idempotent) + browser: /map 85 markers, Special pill hidden, ?falla=1 drawer + visited toggle round-trips on `number` keys, ?hub= description block intact, Next nav 1→3, zero new console errors | signed-in DB sync unverified (Supabase NXDOMAIN); the pill reappearing on `is_special:true` data is a one-line conditional, not browser-tested
- [x] T1.5 One event dataset: official_events.json is now `{hubs, schedule}` — SchedulePage's ~260-line hardcoded scheduleData deleted (schedule + types from src/lib/eventData.ts; event icons are name keys mapped to phosphor components), dead hub `events` refs replaced by `hubId` on schedule events + derived `eventsForHub()` lists (seed derives `hubs.events` from the same linkage; event ids unchanged so URLs/smoke tests keep working) | verified via `npm run verify:schema` ALL SCHEMA CHECKS PASSED (seed derives 5 hubs, idempotent) + node derivation check (20 events / 20 unique ids / 0 dangling hubIds / all 7 icon keys mapped) + browser on dev server: /schedule mar-19 timeline + Sat-14 day switch + row click → ?day&event drawer (icon svg in header, Live badge on Grand Mascletà) + close clears param, deep-link ?day=mar-16&event=castillo-16 opens correct drawer, /map?hub=hub-ajuntament "Events at this location" lists 7 derived rows → click → /schedule?day=mar-14&event=mascleta-14 drawer (also hub-virgen 2 rows), hub-serranos correctly shows no events section (description intact), monument drawers (?falla=1/?falla=3) show no events section, marker open/close ×4 clean + search pill responsive after, zero new console errors (only known pre-existing Radix DialogTitle warnings ×2 = T3.2) | signed-in DB sync unverified (Supabase NXDOMAIN)
- [x] T1.6 README rewritten: real project docs — stack, prerequisites, `npm install --legacy-peer-deps` setup, `.env` walkthrough (every var + where it comes from), Supabase schema+seed (`npm run seed`, idempotent) + `npm run verify:schema` (Docker) + Clerk↔Supabase auth pointer to docs/, commands table, project structure, white-label guide (fallas.json / official_events.json field reference, index.html, design tokens — remaining hardcoded copy honestly listed = T2.8), Vercel + static/tunnel deployment (AUDIT §6) | all code claims cross-checked against seed_supabase.js (hubs from official_events.json w/ derived events), MapComponent fallback, vite engines, .env.example, serve_dist.py; build green + lint clean; docs-only change, no browser flow affected | —
- [x] T1.7 Hub-interaction data loss fixed: PassportView normalises stamps (monument `number` / hub `id`) — the DB select now joins `hubs(id, name)` alongside `fallas(...)` and rows with a NULL `fallas` join no longer crash the map() (one hub check-in used to throw and the catch dropped the ENTIRE passport); MapComponent refreshInteractions + CollectionView sync map `fallas?.number ?? hubs?.id` so hub likes/visits survive the localStorage rewrite; hub stamps/entries render as cards linking to `/map?hub=…`; "explored % of the city" counts monuments only (AUDIT §2) | npm run verify:schema ALL SCHEMA CHECKS PASSED (new mixed-target fixture + resolution assertions incl. the hub-null-fallas crash shape + left-join embed) + build green + lint clean + browser on dev server (signed-out): seeded mixed localStorage → /profile 2 Stamps (monument + HUB plaque) + collection 2 cards with correct /map?falla= & /map?hub= links, hub-stamp click → hub drawer, hub drawer PASSPORT toggle adds hub id to localStorage → toast action → /profile 3 Stamps with the new hub, % stays monument-only (1.3%), /map 85 markers + visited/liked badges intact (hub ids inert), zero console errors | signed-in DB sync paths unverified (Supabase NXDOMAIN — query shapes covered by verify:schema)

### P2 — minor
- [x] T2.1 CSP: `https://clerk-telemetry.com` added to connect-src (Clerk's documented CSP origin) | build green + lint clean + browser: post-edit CSP meta contains the origin, Clerk boots through it (environment/client fetches 200, sign-in form renders, zero CSP violations) | the audit's original violations are NOT reproducible in this environment: instrumented fetch/XHR/sendBeacon + securitypolicyviolation probes on dev AND the live prod bundle show zero clerk-telemetry traffic and zero CSP violations with CDN clerk-js 6.34.1 (telemetry absent or config/sampling-gated since the audit) — so the "kills the console errors" effect is unverified; entry added defensively per AUDIT §1
- [x] T2.2 /profile signed-out state: hero gated on Clerk readiness — signed-in renders the photo (guarded on `user.imageUrl`), signed-out renders a fingerprint placeholder + sign-in CTA card (/sign-in, /sign-up; the local-first passport sections below keep working from localStorage), Clerk-loading state renders pulse placeholders (no `<img src={undefined}>` flash, no CTA flash) (AUDIT §1 `UserProfile.tsx:19`) | verified via `npm run build` + `npm run lint` + browser on dev server signed-out: zero `<img>` on the page, h1 "Your journey", CTA card + SIGN IN/CREATE ACCOUNT buttons navigate to /sign-in (Clerk form renders) and /sign-up, seeded localStorage journey still renders 2 Stamps, zero console errors (only Clerk dev-keys warning) | signed-in hero render unverified (no test credentials); Clerk-loading skeleton is code-reviewed only (transient, not caught in browser)
- [x] T2.3 Loading screen gated on real readiness: fixed 1.2s timer replaced by `document.fonts.ready` + window `load` (App effect) + Clerk client readiness (`ClerkReadyGate` inside ClerkProvider reporting `useUser().isLoaded` up to the splash above it), 300ms minimum display (no flash) + 3s failsafe force-reveal (stalled asset or dead Clerk CDN can never trap visitors) (AUDIT §1 `App.tsx:54-60`); LoadingScreen progress bar 1.2s → 0.6s so it completes before a fast reveal | build green + lint clean + browser on dev server: cold boot splash 681ms (hides after load+fonts, not a flat timer), warm reload 462-465ms (300ms floor), Clerk CDN fully blocked → splash releases at 3041ms (failsafe fires) with navbar + home still rendering, route change / → /schedule does NOT re-show the splash, zero console errors on normal boots | the blocked-Clerk case surfaces 2 pre-existing Clerk loader unhandled rejections ("Failed to load Clerk JS") — unchanged from before this task (T2.4-adjacent)
- [x] T2.4 Clerk key fail-fast: hardcoded fallback publishable key removed from App.tsx — a missing/empty `VITE_CLERK_PUBLISHABLE_KEY` now throws an actionable error at module load ("Missing VITE_CLERK_PUBLISHABLE_KEY — set it in .env (see .env.example). Refusing to fall back to a hardcoded Clerk instance.") instead of silently booting on someone else's dev instance (AUDIT §1 `App.tsx:23-25`; the old `if (!PUBLISHABLE_KEY) throw` guard was dead code under the always-truthy fallback) | build green + lint clean + browser: keyless dev instance (shell-override empty var on :5174, `.env` untouched) fails fast exactly as designed (uncaught error carries the full message, `#root` stays empty, no half-booted app); normal instance (:5173, real `.env`) boots unchanged (home + navbar render, zero console errors) | verified before the edit that `.env`'s var is non-empty and byte-identical to the removed fallback (awk length=54 + grep match — value never printed), so this deployment is unaffected; keyless case is a white screen + console error by design (fail fast)
- [x] T2.5 Hooks merged into one parameterized `useCommunityContent(type, id)` — useFallaDetails/useEventDetails deleted; monument/hub/event map to falla_id/hub_id/event_id (monument keeps number→`fallas.id` resolution, queries/mutations equivalent to the old hooks); state now resets per target so an unresolvable target shows the empty state instead of inheriting the previous target's content. Found + fixed during verification: on in-app target switch the reused drawer showed the PREVIOUS target's comment while React state was already correct (fiber reads proved hook state `["Mock comment for dbid-3"]` vs DOM text `…dbid-1`; forced re-render snapped it; fresh mounts always correct) — fixed by keying FallaDetails and EventCommunityHub per target (fresh mount per switch) and dropping the comment list's AnimatePresence `popLayout`/exit/layout dance | verified via browser on dev server with an instrumented fetch shim mocking Supabase REST per target (dead backend): ?falla=1 renders its mock comment+image, Next → ?falla=3 shows #3's content (no staleness), unresolvable ?falla=4 → empty state + no stale content, ?hub=hub-ajuntament fires hub_id=eq queries + renders hub content, schedule ?event=mascleta-14 fires event_id=eq queries, in-app event switch mascleta-14→pyro-spectacle-14 shows the new event's content, drawer open/close cycles clean, zero new console errors (pre-existing Radix DialogTitle = T3.2) | signed-in write paths (addComment/addImage/toggleImageLike) unverified as always (Supabase NXDOMAIN); root cause of the dropped DOM update on reused instances not fully explained (state fresh, DOM stale, all nodes connected) — keyed remount is the verified mitigation
- [x] T2.6 Photo upload: shared `src/lib/photoUpload.ts` pipeline used by FallaDetails + SchedulePage's EventCommunityHub — client-side validation (JPEG/PNG/WebP/GIF/AVIF, ≤10MB, empty-file check, extension fallback when type is blank), safe filename (`[a-zA-Z0-9_-]` prefix sanitising + extension derived from name or mime — the old `name.split('.').pop()` produced garbage extensions), delete-on-failure (storage object removed when the DB row insert fails, incl. when saveRow throws), and the file input now resets after every attempt so the same file can be re-picked (FallaDetails never reset it) | verified via node harness 32/32 (validation matrix incl. 10MB boundary both sides, upload-fail → no remove, saveRow-fail → remove called with the uploaded path, saveRow-THROW → remove still attempted, path/ext sanitising) + browser on dev server: 12MB file in the ?falla=1 drawer → "Photo is 12.0MB — the limit is 10MB" toast + input reset, pdf → "Unsupported file type", valid jpeg signed-out → "Please sign in to upload photos" (never reaches upload), same rejection toast on the schedule event-hub input, new accept= list on both inputs, drawer Next/Prev/close cycles clean, zero console errors | signed-in upload E2E + the delete-on-failure path against real Supabase unverified (NXDOMAIN — logic covered by the harness)
- [x] T2.7 Bundle: route-level React.lazy (landing stays eager, 8 routes load on demand via per-route Suspense with a pulse fallback) + manualChunks in vite.config.ts (`mapbox`/`clerk`/`supabase`/`vendor`); mapbox-gl CSS import moved App→MapComponent (its JS stub landed in the mapbox chunk and dragged the whole 1.7MB chunk into the eager graph via modulepreload) | verified via build output: the 3,192.55kB single chunk is gone — eager = index 35.64kB + vendor 1,076.92kB + clerk 87.55kB + supabase 168.05kB (~1.37MB / 383kB gzip vs 883kB gzip before), mapbox 1,696.13kB (466kB gzip) + its 41kB CSS now fetch only with /map (home page: modulepreload = vendor/clerk/supabase only, zero mapbox requests), route chunks 1.4–33kB | browser on the BUILT dist (:4173): all 9 routes render after lazy load (home, /map 86 markers + tiles + ?falla=1 deep-link drawer + marker click → ?hub=hub-ajuntament hub drawer, /schedule, /archive, /contact, /profile, /sign-in, /sign-up, /dashboard), search autocomplete "serr" → 3 results, zero new console errors (only pre-existing headless geolocation denial) | fallback flash on a genuinely slow network unmeasured (localhost); the ~1MB vendor chunk could be split further (heroui/framer) if wanted
- [ ] T2.8 White-label: extract festival copy/title/branding into per-tenant config (index.html title, hero copy, header counts)
- [ ] T2.9 MascletaCountdown: compute against Europe/Madrid, not browser TZ
- [ ] T2.10 Remove unused deps (openai, react-transition-group, mapbox-gl-animated-popup, yet-another-react-lightbox)
- [ ] T2.11 fallas.json: 11 entries have placeholder `{lat: 0, lng: 0}` coordinates (numbers 8, 122, 129, 144, 171, 177, 181, 309, 310, 325, 375 — from `scripts/fallatojson.py`'s "placeholder lat/lng" never re-geocoded) → markers land at Null Island and flyTo/Directions go there; re-geocode those streets (scripts/geocode.py)

### P3 — polish
- [ ] T3.1 ICS export: fix DTEND rollover for 23:59 events; add DTSTAMP/UID
- [ ] T3.2 Drawer a11y: add Drawer.Description (kills Radix warnings ×4)
- [ ] T3.3 Search highlight regex lastIndex bug; blur-timeout race → pointerdown handlers
- [ ] T3.4 Delete dead code (CountBtn, primitives, BackgroundGradient, background-beams, tracing-beam, infinite-moving-cards, bento-grid, config/site.ts, DashboardLayout.tsx — newly found: unrouted, stray debug log; fixed for lint in T1.3 but still dead)
- [ ] T3.5 Sweep 30 `any`/`@ts-ignore` sites; type JSON imports + hook returns
- [ ] T3.6 Misc: hardcoded "80 Monuments" count; numeric falla-number sorting; single map-effect cleanup; window.innerWidth in render

## Out of scope tonight (record, don't do)

- Dependency upgrades, build-tooling changes, wholesale rewrites
- Anything requiring production secrets (Vercel account, service-role keys)
- Merging to `main` / production deploys (human decision in the morning)

## Blockers / notes

- **BLOCKER — Supabase project dead**: `pkiqdgbttgngaeespiqy.supabase.co` is NXDOMAIN
  (verified: dig + curl, direct and via 1.1.1.1). All community features (comments, photos,
  likes, interactions, moderation) cannot work against ANY project until the user either
  revives this Supabase project or points `.env` at a new one (then run `scripts/schema.sql`
  + seed; also update the Vercel env). Until then: T0.2 ships the schema, T1.1 gives the app
  an honest offline state. USER DECISION REQUIRED in the morning.
- **Mapbox token — RESOLVED**: real token in `.env`; production bundle verified to contain the
  identical token (in-script sha256 match). The earlier "placeholder" observation was secret
  redaction in tool output, not a real broken deploy. No map-token work needed.
- **Live preview**: https://fallamap.muiry.co.uk serves `dist/` from this Mac (LaunchAgent
  `com.tommuir.fallamap` → serve_dist.py on 127.0.0.1:4173, routed by the `muiry-mac`
  Cloudflare tunnel). OPS RULE for every shift: after a verified change run `npm run build`
  so the live site reflects the branch (dist/ is rebuilt, server picks it up instantly).

## Worklog

<!-- newest entries at top; format: HH:MM — what changed | what was verified | what remains -->
- 12:44 — T2.7: bundle split — route-level React.lazy (HomePage eager, 8 routes lazy with per-route Suspense + pulse fallback) + vite manualChunks (mapbox/clerk/supabase/vendor); mapbox-gl CSS import moved from App.tsx to MapComponent.tsx because its JS stub landed in the mapbox chunk and modulepreload dragged the whole 1.7MB chunk into the eager graph (observed: mapbox fetched on the home page; after the move, zero mapbox requests until /map) | build: 3,192.55kB single chunk → eager index 35.64kB + vendor 1,076.92kB + clerk 87.55kB + supabase 168.05kB (383kB gzip vs 883kB before), mapbox 1,696.13kB + 41kB CSS only with /map, route chunks 1.4–33kB; lint clean | browser on built dist :4173: all 9 routes render after lazy load (/map 86 markers + tiles + deep-link ?falla=1 drawer + marker click opens hub drawer, /schedule, /archive, /contact "GET IN TOUCH", /profile "journey", /sign-in "welcome", /sign-up form, /dashboard "Access"), search autocomplete works ("serr" → 3 results), zero new console errors | an idle-preload warmup was tried and REVERTED (it made Vite's preload polyfill fetch every chunk incl. mapbox right after boot); slow-network fallback flash unmeasured; vendor could be split further (heroui/framer)
- 12:25 — T2.6: shared photo-upload pipeline `src/lib/photoUpload.ts` (used by FallaDetails + SchedulePage EventCommunityHub): client-side validation (JPEG/PNG/WebP/GIF/AVIF, ≤10MB, rejects empty files, extension fallback for blank mime), safe storage names (prefix sanitised to [a-zA-Z0-9_-], extension from name-or-mime instead of the old `split('.').pop()` garbage), delete-on-failure (storage object removed when the DB row fails to save — also when saveRow throws, not just resolves {error}), file input resets after every attempt (FallaDetails never reset it, so re-picking the same file did nothing) | node harness 32/32 on the esbuild-bundled module (validation matrix incl. 10MB boundary; upload-fail → no remove; saveRow-fail → remove with the uploaded path; saveRow-throw → remove still attempted; path/ext sanitising) + browser on dev server: ?falla=1 drawer 12MB jpeg → "Photo is 12.0MB — the limit is 10MB" toast + input reset, pdf → "Unsupported file type", valid jpeg signed-out → "Please sign in to upload photos" (stops before upload), same rejection toast on the schedule event-hub input (mascleta-14), accept= lists on both inputs, drawer Prev/Next/close cycles clean, zero console errors | signed-in upload + real delete-on-failure against Supabase unverified (NXDOMAIN — covered by harness only)
- 11:27 — T2.5: useFallaDetails/useEventDetails merged into one parameterized `useCommunityContent(type, id)` (monument→falla_id with number→uuid resolution, hub→hub_id, event→event_id; queries/mutations equivalent to the old pair; state resets per target so an unresolvable target shows the empty state instead of inheriting the previous target's content) + found-by-verification display bug fixed: on in-app target switch the reused drawer showed the PREVIOUS target's comment while React state was already correct (fiber reads: hook state ["Mock comment for dbid-3"] vs DOM p text "…dbid-1"; forced re-render snapped it; fresh mounts always correct) — fixed by keying FallaDetails (MapComponent: key=number||id||name) and EventCommunityHub (key=event.id) so each target mounts fresh, and removing the comment list's AnimatePresence popLayout + exit/layout dance | verified on dev server with a fetch shim mocking Supabase REST per target: monument #1→#3 switch shows #3's comment+image, unresolvable #4 → empty state with no stale content, hub drawer hub_id=eq queries + hub comment renders, schedule event_id=eq queries + deep link, in-app event switch mascleta-14→pyro-spectacle-14 shows the new event's comment, map drawer Next/open/close cycles clean, build green + lint clean | signed-in write paths unverified (Supabase NXDOMAIN); the dropped-DOM-update root cause is not fully explained (state fresh, DOM stale, nodes all connected) — the keyed remount is the verified mitigation
- 10:20 — UI layering fixes (user-reported "buggy element locations"): (1) CommunityOfflineBanner hid behind the fixed navbar pill — it's `position:static` at y=0 while the nav is `fixed z-[100]` covering 0–124px, so its text was cut in half; now `mt-[76px] md:mt-[124px]` matches the nav height. (2) SignInPage/SignUpPage centered cards sat under the nav+banner; both get `pt-[96px] md:pt-[140px]`. (3) MascletaCountdown's `fixed z-[90] bottom-24 right-6` covered the auth submit button; now returns null on /sign-in and /sign-up. (4) Clerk's neobrutalism theme set `cl-formFieldRow__password` to `position:absolute` (observed: 177px wide, stacked ON TOP of the email field, top 522 vs email 538) — appearance gains `formFieldRow: "!static !w-full"` | build green + lint clean (0 problems) + browser on live site after `npm run build`: nav 0–124, banner 124–160 (no overlap), sign-in inputs both static 402px at top 600/670, sign-up 600/686, countdown absent on auth pages, vision check on screenshot confirms no text cut off and no widget covering content | desktop-width only; mobile widths not re-measured after the fix (prior mobile measurements showed no overlap)
- 10:05 — T2.4: Clerk key fail-fast — App.tsx's hardcoded fallback publishable key removed (it silently booted a hardcoded dev instance when `VITE_CLERK_PUBLISHABLE_KEY` was missing, making the throw guard below dead code); a missing/empty key now throws an actionable module-load error naming the var and pointing at .env.example | build green + lint clean + browser both directions: keyless instance (VITE_CLERK_PUBLISHABLE_KEY= shell override on :5174, .env untouched) throws exactly the designed message (captured via onerror: "Missing VITE_CLERK_PUBLISHABLE_KEY — set it in .env (see .env.example). Refusing to fall back to a hardcoded Clerk instance.") with #root empty and no half-booted app; normal instance (:5173, real .env) boots unchanged (home + navbar render, zero console errors); pre-edit safety check via awk/grep (values never printed): .env's var is non-empty (len 54) and byte-identical to the removed fallback, so this deployment is unaffected | keyless case is intentionally a white screen + console error (fail fast); other pk_test_/pk_live_ hits in the repo are AUDIT.md's historical record + README's generic placeholder only
- 09:52 — T2.3: boot loading screen gates on real readiness — the fixed 1.2s `setTimeout` is gone; splash now waits for `document.fonts.ready` + window `load` (App effect) AND Clerk client readiness (new `ClerkReadyGate` inside ClerkProvider reports `useUser().isLoaded` up to the splash which lives above ClerkProvider), with a 300ms minimum display (no flash-open) and a 3s failsafe that force-reveals both gates so a stalled asset or dead Clerk CDN can never trap visitors; LoadingScreen progress bar 1.2s → 0.6s so it completes before a fast reveal | build green + lint clean + browser on dev server with timing probe (splash = the z-[200] overlay, watched at 25ms from document start): cold boot splash 681ms (hides just after load+fonts — NOT a flat timer), warm reload 462–465ms (300ms floor respected), Clerk CDN fully blocked (`Network.setBlockedURLs`) → splash releases at 3041ms = failsafe fires and navbar + home still render underneath, / → /schedule route change does not re-show the splash, zero console errors on normal boots | blocked-Clerk case logs 2 pre-existing Clerk loader unhandled rejections ("Failed to load Clerk JS") — same as before this task (T2.4-adjacent, recorded there)
- 09:32 — T2.2: signed-out /profile fixed — hero now gates on Clerk readiness (`isLoaded && isSignedIn`): signed-in renders the avatar photo (guarded on `user.imageUrl`), signed-out renders a fingerprint placeholder + CTA card (SIGN IN → /sign-in, CREATE ACCOUNT → /sign-up; copy notes local data is kept on-device), Clerk-loading renders pulse placeholders so no broken `<img src={undefined}>` or CTA flash can appear; the three local-first sections below are untouched (signed-out localStorage journey keeps rendering) | build green + lint clean + browser on dev server (signed-out, seeded visited ["1","hub-ajuntament"] / liked ["3"]): page contains ZERO `<img>` elements (broken avatar gone), h1 "Your journey", CTA card + both buttons visible and clicking SIGN IN lands on /sign-in ("welcome back" + Clerk form renders), /profile still shows 2 Stamps from the seeded local journey, zero console errors (only pre-existing Clerk dev-keys warning) | signed-in hero render unverified (no test credentials — code path unchanged apart from the imageUrl guard); the transient loading skeleton was not caught in-browser (code-reviewed only)
- 08:38 — T2.1: CSP connect-src gains https://clerk-telemetry.com (Clerk's documented telemetry origin; one-line meta change) | build green + lint clean + browser on dev server after the edit: CSP meta contains the origin, Clerk still boots through it (environment + client fetches 200), sign-in form renders, zero CSP violations, no new console errors | AUDIT §1's original violation could NOT be reproduced: instrumented fetch/XHR/sendBeacon hooks + securitypolicyviolation listeners on dev and the live prod bundle (fallamap.muiry.co.uk) show zero clerk-telemetry requests and zero CSP violations with CDN-loaded clerk-js 6.34.1 — the telemetry calls appear absent/config-gated since the audit, so the error-killing effect of this change is unverified; entry is defensive future-proofing
- 08:29 — T1.7: hub-interaction data loss fixed — PassportView now normalises stamps ({key, number?/hubId?, name, topImage}): DB select joins `hubs(id, name)` beside `fallas(id,number,name,images)`, rows processed with flatMap (monument → stamp w/ topImage, hub → hub stamp, neither → skipped) instead of dereferencing `item.fallas` unconditionally (one hub check-in threw on the null join and the catch silently emptied the ENTIRE passport); local path resolves localStorage keys (monument `number` + hub `id`, FallaDetails `identifier`) against fallas.json/hubs so hub check-ins render as hub stamps linking /map?hub= instead of bogus monument cards; "explored % of the city" counts monument stamps only (hubs could push it past 100%); MapComponent refreshInteractions + CollectionView DB sync select `hubs(id)` and map `fallas?.number ?? hubs?.id` so the localStorage rewrite stops erasing hub likes/visits; CollectionView renders hub favorites as cards ("Hub" label) | npm run verify:schema ALL SCHEMA CHECKS PASSED (new hub-visited fixture + mixed-target resolution do-block incl. the hub-null-fallas crash shape + left-join PassportView embed mirroring PostgREST) + build green + lint clean + browser on dev server (signed-out, Supabase NXDOMAIN): seeded visited ["1","hub-ajuntament"] / liked ["3","hub-ajuntament"] → /profile 2 Stamps (#1 + HUB plaça de l'ajuntament) + collection 2 FAVORITES (#3 + HUB), all four card hrefs correct, hub stamp click → /map?hub=hub-ajuntament drawer (VISITED state read back), hub-serranos drawer PASSPORT toggle → localStorage gains "hub-serranos" → toast action → /profile 3 Stamps incl. torres de serranos hub card, % stays 1.3% (1/80 monuments), /map 85 markers + visited/liked badges intact, zero console errors (only headless geolocation) | signed-in DB sync paths unverified (NXDOMAIN; SQL shapes covered by verify:schema)
- 08:12 — T1.6: README.md rewritten (stock react-vite-ui template → real project docs): what fallamap is + white-label direction, stack, prerequisites (Node 18/20/22 per vite engines, npm --legacy-peer-deps, Docker only for verify:schema), getting started, `.env` walkthrough table (all 5 vars incl. server-side SUPABASE_DB_URL + degradation notes for missing Mapbox/Supabase), database setup (Clerk third-party auth → docs/supabase-clerk-auth.md, `npm run seed` = schema.sql then fallas.json + official_events.json seed, idempotent; `npm run verify:schema` Docker harness), commands table + verification standard, project structure map, white-label guide (fallas.json / official_events.json {hubs,schedule} field-by-field incl. hubId + URL id semantics, index.html title/fonts/CSP, --falla-* tokens; T2.8 leftovers listed honestly), deployment (Vercel w/ existing vercel.json + env vars; serve_dist.py + Cloudflare tunnel = how fallamap.muiry.co.uk runs) | claims cross-checked against seed_supabase.js (hub seeding w/ derived events), MapComponent mapUnavailable fallback, vite package engines, .env.example, serve_dist.py docstring; build green (tsc+vite), lint clean | docs-only — no runtime change; T2.8 copy extraction still pending
- 07:18 — T1.5: one event dataset — official_events.json restructured to {hubs, schedule} and SchedulePage's ~260-line inline scheduleData deleted (imported from new src/lib/eventData.ts with ScheduleEvent/ScheduleDay types; event `icon` fields are name keys mapped to phosphor components in EVENT_ICONS, `color`/`isLive` preserved verbatim; event ids unchanged); the dead hub `events` refs (e.g. "mascleta"/"crida" matching no schedule id, never read) are gone — schedule events carry `hubId` (assigned only where the event's `location` names the hub's venue; Town Hall/Calle de la Paz/etc. left unlinked) and hub→event lists derive via eventsForHub(); map hub drawer gained an "Events at this location" list linking to /schedule?day=&event=; seed derives hubs.events from the same linkage | npm run verify:schema ALL SCHEMA CHECKS PASSED (seed 80 fallas/5 hubs, idempotent), node derivation check 20 events/20 unique ids/0 dangling hubIds/7 icon keys all mapped, build green (tsc+vite), lint CLEAN; browser on dev server: /schedule 6 day tabs + mar-19 timeline (5 events incl. Live badge) + Sat-14 switch + row-click drawer (icon svg renders) + close, deep-link drawer, /map?hub=hub-ajuntament 7 derived rows → click lands on /schedule?day=mar-14&event=mascleta-14 with drawer open, hub-virgen 2 rows, hub-serranos honest empty (no section), monument drawers event-free, marker open/close ×4 + search responsive, zero new console errors (pre-existing Radix DialogTitle ×2 = T3.2) | signed-in DB sync unverified (Supabase NXDOMAIN); ICS/share flows not click-tested this run (unchanged code paths, only retyped)
- 06:05 — T1.4: fallas.json data model + number-keyed interaction sync — all 80 entries gain `id` (`falla-<number>`)/`description`/`is_special`/`is_burnt` (honest empty/false defaults, NO fabricated real-world data; fields now mirror the DB columns), seed upserts the new columns, MapComponent merges DB rows over local per `number` with field-level fallback, "Special" filter pill hidden unless some POI is flagged (was permanently "0 of 85"), description block renders for any POI with one; FallaDetails user_interactions reads/writes now go through useFallaDetails's `number`→DB-id resolution (`dbId` exposed) instead of `falla.id` (was `eq('falla_id', undefined)` + `falla_id: undefined` inserts) — also fixed a found-by-the-way bug: `internalId` was never reset, so a monument missing from the DB inherited the previous monument's row id | npm run verify:schema ALL SCHEMA CHECKS PASSED (new enriched-columns + number-resolved read/insert/delete cases; seed idempotent 80 fallas/5 hubs); build green (tsc+vite); lint CLEAN (0 problems); browser on dev server: /map 85 markers (80 fallas + 5 hubs) + map canvas + Special pill hidden + pills All/Events/Liked/Visited, ?falla=1 drawer (badge "Monument #1", name, offline copy, empty-description block correctly absent), visited toggle round-trips on `number` keys ([\"1\"]→[] with button flip both ways), ?hub=hub-ajuntament "About this location" block intact, Next nav ?falla=1→?falla=3 (gap-tolerant), zero new console errors (only headless geolocation denial) | signed-in DB sync unverified (Supabase NXDOMAIN); Special-pill reappearing on `is_special:true` data untested (one-line conditional); NEW BUG logged as T2.11: 11 fallas carry {lat:0,lng:0} placeholder coordinates (geocode never re-run)
- 04:58 — T1.3: ESLint 9 flat config migration — eslint.config.js replaces .eslintrc.cjs, lint script drops flat-config-invalid --ext, and the missing lint devDeps installed (typescript-eslint, eslint-plugin-react-hooks/-refresh, globals — the old config referenced them but they were never declared); all findings fixed: 3 unused catch bindings + prefer-const + unused catch param, stale @ts-ignore removed (react-photo-view ships ./dist/index.d.ts now — verified in node_modules), DashboardLayout redirect gated on isLoaded with real deps + stray debug log removed (file is unrouted dead code → noted under T3.4), justified inline suppressions for mount-only effect deps (marker effect setSearchParams, dead marquee effect) and shadcn/context export patterns | npm run lint CLEAN (0 problems, --max-warnings 0, exit 0 — first working lint in this repo); build green (tsc+vite, confirms the @ts-ignore removals were safe); browser smoke: /map 86 markers + ?falla=1 drawer, /schedule timeline render, zero new console errors | deviations documented in eslint.config.js: no-explicit-any off until T3.5, react-compiler-era react-hooks rules not enabled (set-state-in-effect ×9 etc. = behavioural refactor, not config migration)
- 04:39 — T1.2: moderation pipeline is real — hooks (useFallaDetails/useEventDetails) insert status:'pending' (toasts now say "awaiting review"; FallaDetails upload also surfaces insert failures), admin queues fetch only when `isLoaded && isAdmin` and status flips confirm the updated row before removing it from the list; RLS enforces admin server-side (public.is_admin() on Clerk claim public_metadata.role: full reads + status-only updates via column-level UPDATE grant; non-admin inserts forced to 'pending'; owners cannot self-approve or edit content) | npm run verify:schema ALL SCHEMA CHECKS PASSED incl. new cases (admin sees/updates others' queue rows, admin cannot rewrite text or impersonate, non-admin cannot moderate, forged top-level role claim grants nothing); build green (tsc+vite); browser on dev server: signed-out /dashboard fires ZERO moderation queries + zero console errors (was 2 + errors), Access Denied intact, /map?falla=1 drawer + offline states unchanged | live signed-in moderation E2E unverified (Supabase NXDOMAIN; also needs the Clerk session-token claim setup in docs/supabase-clerk-auth.md); lint unchanged (pre-T1.3)
- 03:35 — T1.1: src/lib/backendStatus.ts reachability store (HEAD /rest/v1/ probe: any HTTP response = online, network failure/timeout = offline; 45s re-probe + browser 'online' event) + CommunityOfflineBanner (global, role=status) + FallaDetails empty states swap "be the first" copy for "community features are offline right now" when offline | build green (tsc+vite); browser on dev server (NXDOMAIN backend): banner + offline drawer copy render, stubbed-success probe clears banner and restores normal copy, rejecting probe brings both back — all three states observed, zero new console errors | live-backend online path unverified (needs live Supabase); lint still pre-T1.3-broken (unchanged, not worse) 
- 03:27 — T0.3: Clerk↔Supabase auth wired (src/lib/supabase.ts `accessToken` callback + src/lib/SupabaseAuthBridge.tsx registering Clerk `session.getToken()` — plan's 'supabase' JWT template is deprecated since Apr 2025, deviation documented in docs/supabase-clerk-auth.md incl. one-time third-party-auth setup) + scripts/schema.sql RLS rewritten keyed on auth.jwt()->>'sub': owner-only writes (comments/images/image_likes/interactions), private/pending reads owner-only, interactions fully private, storage uploads require auth; harness stubs got auth.jwt(), smoke tests got sub-keyed positive+negative RLS cases | npm run verify:schema ALL SCHEMA CHECKS PASSED (owner writes pass; cross-user insert/update/delete rejected; private/pending invisible cross-user; anon sees approved-public only, cannot write); build green (tsc+vite); browser on dev server: accessToken runtime probe verified all 4 paths (no getter→anon key, token→Bearer pass-through, throwing/null getter→anon fallback), /map 85 content markers (80 fallas+5 hubs) + ?falla=1 drawer details + open/close, zero new console errors | signed-in E2E vs live Supabase still blocked (NXDOMAIN); moderation queue blocked until T1.2 adds admin JWT-claim override (update policy now owner-only) 
- 02:17 — T0.2: scripts/schema.sql migration (fallas/hubs/comments/images/image_likes/user_interactions/contact_submissions, event_id/hub_id columns, FKs for every embedded select, community-content bucket + policies) + seed script now applies schema first (pg devDep, npm run seed); npm run verify:schema local harness | throwaway Postgres 16 run: schema applies clean + idempotent ×2, seed = schema → 80 fallas + 5 hubs and safe to re-run, smoke tests mirror all app query shapes (likes count embed, fallas(number), PassportView nested fallas→images, hub embed, anon read / authenticated write RLS, one-target CHECK + duplicate-interaction guard) — ALL SCHEMA CHECKS PASSED | RLS is an INTERIM baseline (open writes) — T0.3/T1.2 tighten to Clerk sub + admin; live-project run still blocked (Supabase NXDOMAIN)
- 02:05 — T0.1: ErrorBoundary around Routes (src/components/ui/ErrorBoundary.tsx) + map init gated on mapboxgl.supported() with try/catch and static "Map unavailable / needs WebGL" fallback | browser-verified on dev server: normal /map 85 markers + ?falla=1 drawer/details + 0 console errors; boundary verified via temporary throw route (removed before commit) — fallback card, navbar intact; WebGL blocked via injected getContext shim → fallback rendered, app shell + search→drawer still work, 0 uncaught errors | —
- 01:55 — Deep QA audit complete (AUDIT.md: 3 P0, 9 P1, 13 P2/P3 + verified-working list); findings folded into PLAN.md as T0.1–T3.6 | audit itself browser-verified all 9 routes; Supabase NXDOMAIN claim re-verified independently | first grind shift picks up P0 at 02:04
- 01:40 — Hosted at https://fallamap.muiry.co.uk: serve_dist.py (SPA-fallback static server) + LaunchAgent + cloudflared ingress for `muiry-mac` tunnel | 200s on /, /map, /schedule through public HTTPS (verified via Cloudflare edge); tickets.muiry.co.uk unaffected | local DNS cache on this Mac may lag a few minutes
- 01:33 — Mapbox token added to .env; production bundle token verified identical via in-script hash compare | both hashes compared in-script | AGENTS.md token note correction pending user approval; audit in flight
- 01:15 — PLAN.md, AGENTS.md, .env/.env.example created; branch `overnight-polish` cut | build passes on unmodified main snapshot | audit in flight
